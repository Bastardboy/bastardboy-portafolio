<nav class="absolute top-6 right-6 z-20 text-sm md:text-base">
  <!-- Botón de menú hamburguesa -->
  <button
    class="block md:hidden text-white focus:outline-none"
    id="menu-toggle"
    aria-label="Abrir menú"
  >
    <svg
      xmlns="http://www.w3.org/2000/svg"
      class="h-6 w-6"
      fill="none"
      viewBox="0 0 24 24"
      stroke="currentColor"
      stroke-width="2"
    >
      <path
        stroke-linecap="round"
        stroke-linejoin="round"
        d="M4 6h16M4 12h16M4 18h16"></path>
    </svg>
  </button>

  <!-- Menú de navegación -->
  <div
    id="menu"
    class="hidden md:flex flex-col md:flex-row space-y-4 md:space-y-0 md:space-x-6 bg-gray-800 md:bg-transparent p-6 md:p-0 rounded md:rounded-none absolute md:static top-14 right-6 md:top-0 md:right-0 w-60 md:w-auto shadow md:shadow-none"
  >
    <a href="#aboutme" class="block text-white font-semibold nav-link"
      >Acerca de mí</a
    >
    <a href="#technologies" class="block text-white font-semibold nav-link"
      >Tecnologías</a
    >
    <a href="#projects" class="block text-white font-semibold nav-link"
      >Proyectos</a
    >
    <a
      href="#contactme"
      class="block bg-white text-black rounded-full px-4 py-2 font-semibold shadow hover:bg-gray-100 transition"
    >
      CONTÁCTAME
    </a>
  </div>
</nav>

<script>
  document.addEventListener("DOMContentLoaded", () => {
    const menuToggle = document.getElementById("menu-toggle");
    const menu = document.getElementById("menu");

    menuToggle.addEventListener("click", () => {
      menu.classList.toggle("hidden");
    });
  });
</script>
