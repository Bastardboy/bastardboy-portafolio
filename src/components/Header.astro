<nav class="absolute top-6 right-6 z-50 text-sm md:text-base">
  <!-- Botón hamburguesa -->
  <button
    class="block md:hidden text-white focus:outline-none"
    id="menu-toggle"
    aria-label="Abrir menú"
  >
    <svg
      xmlns="http://www.w3.org/2000/svg"
      class="h-6 w-6"
      fill="none"
      viewBox="0 0 24 24"
      stroke="currentColor"
      stroke-width="2"
    >
      <path
        stroke-linecap="round"
        stroke-linejoin="round"
        d="M4 6h16M4 12h16M4 18h16"></path>
    </svg>
  </button>

  <!-- Menú -->
  <div
    id="menu"
    class="hidden flex flex-col md:flex md:flex-row items-start md:items-center gap-4 bg-gray-800 md:bg-transparent p-4 md:p-0 rounded-lg md:rounded-none absolute md:static top-12 right-0 md:top-0 md:right-0 w-64 md:w-auto shadow md:shadow-none transition-all"
  >
    <a href="#aboutme" class="text-white font-semibold hover:underline">
      Acerca de mí
    </a>
    <a href="#technologies" class="text-white font-semibold hover:underline">
      Tecnologías
    </a>
    <a href="#projects" class="text-white font-semibold hover:underline">
      Proyectos
    </a>
    <a
      href="#contactme"
      class="bg-white text-black rounded-full px-4 py-2 font-semibold shadow hover:bg-gray-100 transition"
    >
      CONTÁCTAME
    </a>
  </div>
</nav>

<script>
  document.addEventListener("DOMContentLoaded", () => {
    const menuToggle = document.getElementById("menu-toggle");
    const menu = document.getElementById("menu");

    if (menuToggle && menu) {
      menuToggle.addEventListener("click", () => {
        menu.classList.toggle("hidden");
      });
    }
  });
</script>
